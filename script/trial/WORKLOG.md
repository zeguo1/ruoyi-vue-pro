# 知办官网试用开户：实施及验收记录

## 基线（2026-09-14）
- 后端基线 2496a7b392；已有 OpenAPI v6 修复保留。未发现 /、/opt、/opt/mgs 或两个仓库下适用的 AGENTS.md。
- 运行容器 mgs199-server-1 健康；compose 发布 web-antd。不得覆盖运行挂载的 server target JAR / web-antd dist。
- CRM 内部线索、客户、跟进服务和系统租户/用户/角色服务已存在；createTenant 会初始化内部管理员。
- 当前工作只修改 MGS；不部署、不写真实业务，不改变知办公共助手限制。

## 原始范围及证据门槛
- [ ] 可信申请身份、确认、归属、防重放、幂等 CRM 留资。
- [ ] 最小业务角色、普通用户、虚构客户与数据隔离。用户最新指定共享演示租户 1，另建栖云运营租户。
- [ ] 持久化逐步开户、外部超时先核对、并发/重启恢复、无长事务跨网络。
- [ ] 知办确定性适配器、个人授权、安全卡片依赖、缺配置不可开户。
- [ ] 绑定/首次业务事件幂等及运营跟进；不得泄露凭证。
- [ ] 两侧到期撤权、令牌/会话撤销、失败重试、保留数据。
- [ ] web-antd 运营列表/详情/恢复及权限、菜单迁移。
- [ ] SQL、配置、实际生成 OpenAPI、工具契约、联调与回滚说明。
- [ ] 隔离集成测试涵盖用户要求所有验收用例及 CRM/登录回归。
- [ ] 标明替身与真实验证边界。完整链路必须由真实公众号业务闭环证明。

## 待外部确认
演示租户已确认是 1，无访客套餐。尚需实际 HTTPS 环境地址、栖云企业初始化联系人/权限/有效期/容量、试用期限/配额、知办内部接口契约和服务密钥配置。已集中询问；开发采用关闭的配置，测试使用显式替身。以下历史进度按时间追加，以末尾最新阶段为准。

## 用户补充（开发中）
租户 1 作为演示，另建“栖云”运营本企业业务。取消体验套餐配置/选择；所有申请及业务办理通过 Agent。共享演示租户必须在接口及记录层隔离，不能只依赖 tenant-id。

## 当前开发进度
- 新增增量 SQL：申请、步骤、nonce、账号映射、事件、业务幂等记录。尚未应用到运行环境。
- 新增服务签名、可信主体归属、持久化本地步骤及外部查询优先恢复、到期两侧独立撤销。
- MGS 本地实现复用 CRM 线索/客户与用户角色服务，共享演示租户且不交付管理员。
- 新增专属体验业务接口、共享租户 API 白名单、事件回调和运营接口；web-antd 运营/结果查看页已编写。
- 13 个 H2/签名测试通过（本地模拟业务资源与知办替身）；后续新代码仍需重新运行全套测试。

## 下一阶段必须完成（不能据当前状态声称完工）
1. 真实用户/CRM 服务和事务集成验证；共享租户两用户、接口/导出/文件、令牌到期隔离测试。
2. MGS 个人 OAuth 授权安全交换及激活；凭据加密存储/幂等、强制失效，非模型接口。
3. 栖云企业租户初始化的可执行流程、内部负责人；菜单和运行任务的增量初始化。
4. 事件重放/业务证据测试；增加独立实例故障/租约和重启回归。
5. 前端类型检查及浏览器验证；实际生成 OpenAPI、机器工具契约、完整配置与回滚说明。
6. 真实知办对接与公众号安全卡片/全链路验收。当前缺知办内部接口实现及有效配置，不能伪造成功。
- 新增共享租户路由白名单、无写入的状态查询、事件实际业务依据及队列公平性测试后，16 项测试通过。该范围验证 JDBC 状态/策略函数，不能替代真实 Spring Security + MyBatis 数据权限 + OAuth 的集成证明。
- 前端全量 vue-tsc 被 SIGKILL 终止，当时可用内存不足 150 MB；没有重启共享服务。改用 1 GB 堆上限、仅新增文件的 tsconfig.trial.json 检查，不能报告全量类型检查通过。
- 当前源代码（含菜单权限收紧、运营接口、回调及远端地址快照）16 项测试通过。web-antd 新增 API 与两个页面的受限范围 vue-tsc 检查通过；全量检查未通过（系统 SIGKILL），浏览器验证尚未执行。
- 2026-09-14 用户再次确认：双系统开户业务流程应在 MGS。实现继续由 TrialOrchestrator 作为唯一流程编排，知办仅执行内部能力/可信身份/安全交付。

### 恢复开发的具体位置
- 两个仓库分支均为 feat/knowdo-trial-onboarding，当前变更尚未提交/部署。
- 验证命令 script/trial/run-tests.sh；前端 NODE_OPTIONS=--max-old-space-size=1024 pnpm exec vue-tsc --noEmit --skipLibCheck -p apps/web-antd/tsconfig.trial.json。
- 关键未完成：安全授权交换不可用（HttpKnowdoTrialAdapter 只有确定性 lookup/ensure 客户端）；应复用现有 OAuth2TokenService，把令牌只交付给有独立能力的签名服务接口，不进入模型结果。需要幂等授权资源引用/到期撤销回归。
- 激活/安全卡片领取、绑定凭证转发/重复/过期的替身和真实测试仍缺；不要因 adapter COMPLETE 就宣称这些验收已完成。
- 新栖云租户尚未创建；需实现可执行内部初始化，避免向访客交付租户管理员。没有确认的运营 ID/期限/配额/服务凭据仍为空。
- 现有测试调用的是 JDBC 状态机、访问策略及模拟业务服务。尚缺真实 MVC 过滤链、MyBatis CRM/用户/角色服务和 OAuth 联合验证。
- 本机总内存 16 GB，共享服务已使用约 12 GB；不要同时运行大范围前端与 Java 测试。不得杀共享进程或覆盖正在挂载的 target/yudao-server.jar、apps/web-antd/dist。

## 第二阶段：个人授权交付（2026-09-14）
- 已加入 MGS_GRANT 持久化步骤，复用现有 OAuth2TokenService。crm_trial_authorization 只保存 refresh_token_id 与非秘密引用，不在申请/步骤/CRM 复制凭据。
- 独立 HTTPS + AUTHORIZATION 签名接口 /admin-api/crm/trial-internal/authorization 提供短期访问令牌；不返回刷新令牌，隐藏于 Agent OpenAPI，关闭访问日志并禁止响应缓存。身份、准备步骤、归属、过期和原授权失效均检查。
- 本地准备幂等；授权刷新续用已有 grant，撤销后不会因重试盲目新建授权。固定 OAuth clientId 已加入申请策略快照，避免配置变化把旧申请指向其他客户端。
- 修补试用撤权的刷新令牌残留：现有核心 removeAccessToken(userId,type) 可能在访问令牌行已清理时遗漏刷新会话。试用适配器额外清理该用户全部刷新行和 refresh-as-bearer 缓存。
- 24 项试用测试通过（4 个测试类）；额外运行现有 AdminAuthServiceImplTest 的 17 项及 OAuth2TokenServiceImplTest 的 13 项，合计 54 项通过。使用 H2、明确的 OAuth 替身与项目内嵌 Redis 替身，不能据此声称真实联调通过。见 test-evidence.json。
- 已只读核对 /opt/knowdo/AGENTS.md、website-support-agent-plan.md 及应用源代码关键词；发现对接规划，尚未发现 mgs-trials/四项开户工具的服务实现。不修改知办、不触发其部署规则。待对方提供真实契约及地址后联调。
- 仍需完成：账号激活/安全卡片真实对接、栖云租户内部初始化、实际 MVC+MyBatis 业务隔离测试、生成 OpenAPI/机器契约、浏览器测试和完整交付/回滚文档。完整体验链路尚未验收，任务继续。
- 生成文档时发现全局分组生成器会覆盖 operationId 并统一注入 tenant-id/Authorization。已添加只针对试用服务路径的 OpenAPI 后处理：保留四个工具稳定名称、移除错误身份头、声明 HMAC 服务契约、把 code=0 请求成功与 accountReady=true 整体就绪分开，未修改其他模块默认行为。
- 新增 TrialOpenApiExportTest，直接调用隔离 Springdoc /v3/api-docs/all 并检查结果；内部凭据接口必须不出现在工具目录。导出及机器核对脚本位于 script/trial/generated 和 build-tool-contract.py。生成环境仅初始化文档与空壳控制器，不执行开户或调用实际服务。
- 本阶段最终证据：26 项试用测试、1 项真实 Springdoc 导出测试、30 项现有登录/OAuth 回归，共 57 项通过。导出 12 个接口、全部本地 $ref 可解析；generated/openapi.json 和 generated/tool-contract.json 已按本次导出更新。该版本不是正在运行的生产文档。
- HttpKnowdoTrialAdapter 额外限制完整响应体的 10 秒等待与 16 KiB 大小；超时取消网络请求，避免响应头已返回但正文阻塞导致维护任务悬挂。新增有界响应订阅器测试，真实远端超时仍需联调。
- 剩余优先项未缩减：栖云企业租户初始化、MGS 普通账号的安全激活/登录交付、完整 Spring MVC/MyBatis 数据隔离与业务写入回归、浏览器验收、任务初始化/部署回滚手册、知办真实接口和公众号全链路。
- 检查到现有管理员重置密码操作日志会记录密码哈希，通用异常日志可能记录请求体。因此安全激活不能直接照搬原密码重置管理接口；下一阶段须避免新凭据进入这些记录。

## 第三阶段：真实 CRM 请求隔离与内部企业初始化（2026-09-14）

- 重新检查适用 AGENTS.md（未发现）及当前容器：mgs199-server-1、mgs199-admin-1 健康；发布应用仍为 web-antd，保持运行挂载制品不变。没有执行部署或真实写入。
- 新增 TrialBusinessIntegrationTest 的 8 项隔离测试。真实加载 Spring MVC、TokenAuthenticationFilter、TenantSecurityWebFilter、TenantVisitContextInterceptor、TrialWebConfiguration、方法权限、CrmPermissionAspect、客户/跟进/权限服务、MyBatis 租户过滤和审计字段填充。系统 OAuth 校验、权限目录、字典、用户目录及未调用业务服务为显式替身。
- 实测两名演示用户的客户/跟进结果隔离、禁止指定他人客户/租户/角色/附件字段、真实 CRM 数据权限拒绝、方法权限拒绝、匿名/跨租户/访问租户/到期拒绝，以及并发 HTTP 重试只产生一条业务记录。文件/导出验证到 MVC 哨兵处理器前被拒绝，未连接实际文件存储；不将此扩大为完整文件系统验收。
- 回滚故障注入在真实跟进记录和客户状态已写入后的幂等标记阶段；断言命中特定数据库约束错误、三者都不残留，移除故障后同一幂等请求正常提交。测试最初发现配置顺序使 MyBatis 审计填充未安装，修正测试装配后合法路径和回滚均通过，没有降低数据列约束来掩盖问题。
- 新增 TrialOperatorBootstrapProperties/Service 和 trialOperatorBootstrapJob，以及 V20260914_03__trial_operator_setup.sql。服务器配置固定栖云企业资源，复用既有租户/用户/角色服务；内部菜单快照与访客套餐分离。默认关闭，敏感初始密码只由部署密钥配置进入，任务结果只有资源 ID。
- 企业初始化使用与 TenantService 一致的 DSTransactional 事务和全局 guard，禁止混入外层 Spring 事务；重复调用不重置密码/角色，有同名未登记租户或记录冲突时停止人工核验。5 项测试验证实际 dynamic-datasource 事务、嵌套 Spring 事务的回滚、并发幂等和配置保护；租户/用户服务仍是替身，完整真实初始化需下一阶段验证。
- 本轮 run-tests.sh 通过：39 项试用测试 + 1 项实际 Springdoc 导出。合并此前未改动系统模块的 30 项登录/OAuth 回归证据，test-evidence.json 累计 70 项；各报告时间和覆盖限制均明确。OpenAPI 仍为候选版本 mgs-trial-v1-candidate 的 12 个接口，不包含内部凭据或初始化入口，非运行实例导出。
- 补充 OPERATIONS.md：MGS 独立发布、三份增量迁移、初始化任务配置及受控触发、持续维护、关闭新申请和保留数据回滚。特别禁止活跃体验账号存在时撤掉专属接口保护并直接回滚旧版本。

### 当前剩余项（保持原验收范围）
1. 普通体验用户安全激活/浏览器 MGS 登录交付，以及知办安全卡片转发/重放/过期测试。初始随机密码当前不交付；个人 OAuth 授权不等于浏览器登录。
2. 真实 TenantService/AdminUserService/RoleService/PermissionService 完整开户和撤销联合验证；企业初始化尚未在真实服务链或运行环境执行。
3. web-antd 页面浏览器验收与完整前端检查；当前只完成新增页面限定范围类型检查。
4. 实际 HTTPS 配置、企业联系人/菜单/有效期/容量、试用期限/配额、知办内部服务与安全交付接口。尚未部署、创建栖云租户、开通真实用户或运行公众号业务闭环。
5. 对全目标进行最终代码审查、提交交付和真实链路验收；源代码/替身测试完成不得等同于知办同步或两边已生效。

## 第四阶段：安全登录交付和实际普通账号创建（2026-09-14）

- 新增 TrialLoginVault/TrialLoginDeliveryService 与隐藏的 `/admin-api/crm/trial-internal/login-delivery`。原始随机密码在创建用户的同一事务中加密保存，不再生成后丢弃；现有 AdminUserService 保留 BCrypt 登录哈希，不调用会记录密码重置上下文的管理重置接口。
- AES-256-GCM 使用独立的版本密钥、随机 nonce 和申请/身份/租户/用户绑定信息。新增申请要求加密配置齐全，旧密钥缺失时失败关闭。新增迁移 `V20260914_04__trial_login_delivery.sql`，没有应用到运行环境。
- 私有领取仅允许实际 HTTPS 和 DELIVERY 服务签名代表原主体，全部开户步骤完成、READY、未过期、账号启用、密码未被修改时才交付。请求体仅申请 ID，不接收新密码。相同主体/领取动作允许 5 分钟网络重试，其他领取动作或过期请求拒绝；维护/撤权清除密文但保留登记。结果对象 toString 脱敏，禁用访问日志/no-store，实际 Springdoc 确认该接口及凭据模型不在工具目录。
- 7 项登录交付测试覆盖真实 AES-GCM/BCrypt、并发稳定响应、换主体/领取标识/过期、密文篡改、不同加密密钥轮换、密码重置/撤销后不再交付和密文清除。知办卡片本身尚未实现/联调，不能据此声称绑定凭据防转发验收完成。
- 新增 MgsTrialAccountIntegrationTest，实际加载 AdminUserServiceImpl、RoleServiceImpl、PermissionServiceImpl、MyBatis 和 Spring 事务。复现原实现第二名体验用户因角色名称重复而失败；已将角色名称改为确定性的申请后缀，保留每用户独立 SELF 数据范围及两项最小菜单权限。
- 3 项实际账号测试现已通过：两名普通账号/独立角色与登录密码哈希匹配、撤权不影响另一名用户，以及加密交付保存失败和用户容量不足时回滚用户/角色/角色菜单/账号映射。租户元数据/菜单目录/CRM/OAuth 是该测试的明确边界替身；尚非完整服务全链路。
- 本轮 run-tests.sh：49 项试用测试 + 1 项实际 Springdoc 导出通过；加此前 30 项原登录/OAuth 回归证据，累计 80 项。生成文档仍为 12 个候选公开接口，私有凭据交付不进入 Agent OpenAPI。未部署、未创建真实企业租户或演示用户。
- 更新 KNOWDO_CONTRACT_DRAFT.md/OPERATIONS.md/配置示例。明确 DELIVERY ensure 先创建受原会话保护的卡片引用，READY 后实际领取；不能在 READY 前用试用业务接口成功作为 KNOWDO_AUTH 完成前提，避免流程循环等待。

### 下一阶段仍需完成
1. 真实 TenantService 企业初始化与普通账号、实际 OAuth/登录/CRM 的联合验证，覆盖完整业务服务边界；当前分片集成测试不能替代此项。
2. web-antd 运营页和结果页的浏览器验证；实际登录时选择演示租户并进入本人结果页的体验验证。
3. 知办内部成员/授权/安全卡片及公众号真实接口和配置；卡片仅对原安全会话交付，绑定凭证的转发/重放/过期必须由知办落实和联调。
4. 栖云企业资料/菜单/容量/有效期、试用期限/配额、HTTPS 地址与密钥仍需实际配置。不得据本地密码哈希测试声称已经成功登录浏览器或公众号。
5. 完整交付审查、提交及真实闭环验收。任务保持未完成，当前新开户默认关闭。

## 第五阶段：实际前端浏览器回归（2026-09-14）

- 再次检查仓库及父目录，无适用 AGENTS.md。只读确认 mgs199-server/admin 健康、发布应用仍为 web-antd；保持生产挂载 JAR/dist 不变。
- 使用前端测试调试技能；本环境未提供 Browser 插件，复用已安装 Playwright 1.62.1 与 Chromium headless-shell revision 1243。仅启动 127.0.0.1:5198 的 Vite 开发服务器，完成后已停止自己的 Vite 进程。所有后端 API（包括登录）均由浏览器路由拦截提供虚构 fixture；未连接生产接口。
- 浏览器实测发现并修正：体验账号默认进入示例统计页，改为依据服务端返回的 `mgs_trial_` 角色进入本人结果页，动态菜单不再混入通用分析/个人资料页面；跳过不在试用接口范围内的全局字典、通知和其他业务入口。后端访问保护未放宽，前端角色判断仅用于界面模式。
- 体验结果刷新失败时清除旧客户和跟进，显示可重试提示，消除原未处理 Promise 异常；运营列表/详情失败同样清除旧数据，避免对旧申请继续操作。新增并发读取序号，避免较慢响应覆盖更新的详情。
- 修复手机固定 760px 抽屉使恢复按钮落在屏幕外的问题；抽屉宽度受视口限制，表格可独立横向滚动。恢复结果仍只说明请求受理，不宣称双侧开户完成。普通用户结果页没有新增业务按钮，办理入口继续是公众号 Agent。
- 5 个实际浏览器场景通过：运营桌面、只读桌面、体验桌面、运营手机、体验手机；覆盖分页、详情、单次恢复/按钮权限、错误页面、默认登录落点与页面重载、模拟 Agent 跟进显示、过期错误清除旧结果。桌面 1440×1000、手机 390×1000，无空白/Vite 覆盖层/页面横向溢出。场景断言失败会返回非 0。
- 新增前端 `scripts/trial/browser-smoke.mjs` 和 README。`frontend-test-evidence.json` 保存源文件 SHA256、实际请求路径、逐场景检查结果和覆盖限制；不把模拟 API/登录算作真实账号或公众号验收。
- 已通过 7 个涉及的前端源码和浏览器脚本的 oxlint、`vue-tsc --noEmit --skipLibCheck -p apps/web-antd/tsconfig.trial.json`（1536MB 堆）。没有声称全应用类型检查通过。现有 Ant Design Vue `columnResize` 未声明事件告警、登录表单/存储弃用告警及被隔离规则阻止的外部资源请求保留在证据中；不修改依赖包隐藏告警。
- 后端测试 80 项累计证据与 5 个 UI 场景分别报告；本阶段未部署、未真实登录、未初始化真实栖云租户、未写入真实业务。

## 第六阶段：企业初始化真实服务链（2026-09-14）

- 新增 `TrialCorporateTenantIntegrationTest`，复用隔离账号数据库装配，将 TenantService、TenantPackageService、MenuService 替身替换为实际实现及 MyBatis Mapper；用户/角色/权限同样使用实际服务。显式启用动态事务 Advisor 和 Spring AOP，并断言初始化服务确实被代理。
- 4 项测试通过：真实创建栖云企业和内部管理员，再在既有演示租户 1 创建普通体验用户，确认两侧租户/角色/菜单分离；8 次并发初始化只产生一套企业资源，重复调用不重设密码；最后初始化登记故障使真实内部权限记录、租户、管理员、角色及角色菜单全部回滚；实际菜单被禁用时在资源创建前拒绝。
- 数据源为隔离 H2，事务实际使用 DynamicRoutingDataSource/DSTransactional 及嵌套 Spring Transactional。部门/岗位/配置/MQ/CRM/OAuth 仍是该测试外围替身；没有创建运行环境中的栖云租户，也没有声称 MySQL 或完整认证/业务联合链路已通过。
- 重新执行完整 `run-tests.sh`：53 项试用测试 + 1 项实际 Springdoc 导出检查通过，生成 12 个公开候选接口；加此前未改动系统模块的 30 项登录/OAuth 回归证据，累计 84 项后端测试，5 个浏览器 fixture 场景另列。已刷新逐报告时间和生成文档。

### 尚未完成的验收与交付
1. 以实际认证/OAuth、普通账号和 CRM 连成完整的 MGS 本地业务闭环；当前多组集成测试各有明确服务边界，不能替代统一链路。
2. 知办可信入口、内部成员/个人授权、安全卡片、原会话领取、公众号绑定，以及凭证转发/重放/过期的实际接口联调。
3. 已确认租户 1 为演示、栖云为企业运营；真实企业联系人/菜单/容量/期限、试用期限与配额、服务 HTTPS 地址和密钥仍待配置，所有新开户默认关闭。
4. 真实前后端浏览器登录、部署后的 OpenAPI 和公众号完整体验链路；本轮没有部署、真实业务写入或修改知办项目。
5. 全目标最终代码审查及提交交付。新增代码与证据仍保留在工作树，尚未宣称已推送或上线。

## 第七阶段：实际登录/OAuth/CRM 联合回归（2026-09-14）

- 新增 TrialLocalJourneyConfiguration/Test，把原先分离的 MGS 普通用户创建、安全密码交付、AuthController HTTP 登录、OAuth 客户端和令牌、实际系统权限、CRM 线索/客户/跟进及事件摘要连接到同一隔离 H2 数据库。真实 OAuth Redis DAO 连接每个测试上下文独享的 loopback 随机端口 jedis-mock；Spring 缓存注解启用，权限缓存使用 ConcurrentMapCacheManager。没有访问共享 Redis。
- 5 项联合测试通过：两名普通用户的浏览器登录与个人 Agent 授权结果一致且数据隔离；12 次并发开户只创建一份本地资源/授权；知办授权提交后超时由新编排器实例查询结果续办，不重建 MGS 资源；到期时先拒绝业务，再撤销浏览器/个人授权/已缓存刷新令牌别名，旧密码登录失败且不影响另一账号；绑定/业务事件核验、去重及 CRM 摘要写入失败的整体回滚。
- 知办 lookup/ensure 是明确测试替身，事件也是隔离输入；字典、部门/岗位、配置/MQ及其他业务外围仍有替身。未运行真实官网、知办安全卡片或公众号；对象重建不是 OS 进程崩溃验收。完整边界汇总在 ACCEPTANCE.md。
- 联合测试对齐了旧系统单测 DDL 与仓库正式 SQL 的已确认差异：登录日志 tenant_id 列、浏览器 OAuth grant 可为 null 的 scopes。仅修改新测试数据库装配；没有修改生产 SQL 约束或登录行为来使测试通过。测试配置使用项目已有 Redisson 客户端，没有新增依赖。
- 统一回归现包含 58 项试用测试和 1 项实际 Springdoc 输出；原登录/OAuth 30 项回归本轮重新执行，具体结果及时间见 test-evidence.json。浏览器的 5 个 fixture 场景沿用上一阶段独立证据，不扩大其含义。

### 候选提交与前端门槛（2026-09-14）

- 后端功能、迁移、契约和 89 项通过的测试证据已提交为 `fc1a45c4a4751fec0ca7c4fa3c2a2670cdf9302a`，推送至 origin 的 `feat/knowdo-trial-onboarding`；远端分支 SHA 已读回核对。没有合并或部署。
- 前端完成 ESLint 自动格式/导入排序修正；带类型信息的 Oxlint、ESLint、Stylelint、限定范围 vue-tsc 通过，5 个浏览器场景重跑通过；随后仅做 Oxfmt 空白格式整理并重新通过 ESLint/vue-tsc。前端 10 个文件已暂存，尚未提交。
- `lefthook.yml` 的 checkType 要求 `pnpm check:type`。在独立 systemd 临时任务中用 MemoryMax=2560M / MemorySwapMax=128M 和串行任务运行完整检查：全仓库在 web-antdv-next 触发 oom-kill；单独检查实际发布的 web-antd 同样触发内存上限。没有绕过钩子，也没有声称全量检查通过。临时任务均已结束，仅本地测试进程被限制；生产 JAR/dist 和容器保持不变。
- 前端提交需解决完整检查资源条件，或获得明确的候选提交检查范围例外。真实知办接口、安全卡片、公众号联调和上线配置仍是独立待完成项，不因后端分支已推送而视为生效。

## 第八阶段：独立进程中断与持久化恢复（2026-09-14）

- 新增 TrialProcessRestartTest，父测试进程等待子 JVM 到达确定的业务边界后强制结束该进程，再启动全新 JVM 打开同一临时文件 H2 数据库。使用实际 MGS 账号、角色、CRM、授权、交付加密及编排实现；没有修改生产行为或访问运行数据库。
- 三个场景均通过：MGS 本地事务未提交时中断，用户/角色/交付密文一起回滚；MGS 本地资源已提交、知办创建前中断，恢复保留已提交资源编号；知办成员已提交、MGS 步骤确认前中断，恢复先查询原结果而不再创建。最终一份申请、普通用户、角色、演示客户和授权，既有两个测试租户不增加；远端三个步骤各只 ensure 一次。恢复完成后的重复 advance 仍不产生资源。
- 验证未过期的 RUNNING 租约不能被接管，然后仅在临时测试库将租约到期时间设为过去，加速原本 60 秒等待。生产租约逻辑未变。知办为独立文件数据库、独立提交的明确替身；这不证明 MySQL、真实知办网络、机器断电或部署切换的恢复能力。
- 独立执行新增 3 项测试通过；随后完整 run-tests.sh 再次通过：61 项试用测试 + 1 项 Springdoc，导出 12 个候选公开接口。加此前 30 项原登录/OAuth 测试，累计 92 项；test-evidence.json 保留各报告实际时间，不把此前执行记录表述为本轮重跑。
- 同步修正 ACCEPTANCE.md 和 KNOWDO_CONTRACT_DRAFT.md 的验证边界。只读确认 mgs199-server/admin 等容器仍健康；未部署、未创建真实租户/账号/业务，未改写生产挂载 JAR/dist。
- 前端仍为原有 10 个暂存文件；未收到跳过 checkType 的候选提交例外确认，未绕过钩子。真实官网—公众号闭环及知办安全卡片/渠道撤销仍需外部接口与隔离环境联调，完整目标保持未完成。

## 第九阶段：浏览器连接实际隔离后端与日期显示修正（2026-09-14）

- 新增 TrialBrowserJourneyTest 和 browser-live-journey.mjs。实际 web-antd Vite 浏览器经仅转发传输的隔离代理连接随机 loopback 端口 Tomcat；所有登录/租户/权限/CRM 响应由实际 MGS Controller/服务生成。测试库 H2、jedis-mock、Spring 内存权限缓存独享；知办、字典和外围模块仍为明确替身。没有读取或写入运行实例数据。
- 使用真实 V20260914_02 菜单迁移的页面/按钮层级；测试装配曾把同一前缀注册两次导致登录路由 404，已合并测试路由谓词，未修改生产路径规避问题。
- 将正式 Yudao 时间序列化模块接入测试 HTTP 转换器后，复现体验页直接显示 nextTime 毫秒数，日期可读性断言失败。前端 API 类型改为 number，体验页复用项目 formatDateTime，旧五场景 fixture 也改为数字时间戳；生成 OpenAPI 原本就是 integer/int64，无需改写正确的后端约定。
- 修复后桌面 1440×1000 和手机 390×1000 的两个真实登录浏览器上下文通过：个人 Agent token 写入并幂等重放、页面返回相同记录 ID、刷新/重载保持结果、第二账号只看到自己的客户和空跟进。最终数据库仅一条跟进和幂等记录。页面显示 2027-01-02 10:30:00，等待初始加载层退出后截图，检查无框架错误层/页面溢出/未处理异常。已有表单/存储弃用与主动阻断的外部资源告警保留；不宣称外部图标已验证。
- 本轮增加 1 个显式启用的 JUnit 浏览器专项（内含两个账号场景），累计后端测试报告 93 项；此前 92 项报告保留其原执行时间。本专项不是默认 run-tests.sh 的隐式依赖，无 TRIAL_UI_URL 时跳过，不能将跳过计为通过。
- 日期修复后原五项模拟 API 浏览器回归重跑通过，涉及文件 ESLint/Stylelint/带类型 Oxlint 和限定范围 vue-tsc 通过。前端源码和证据已更新，10 个文件仍暂存且未提交；全量 checkType 例外仍未获确认，也未绕过钩子。
- 新增 BROWSER_JOURNEY.md、browser-live-test-evidence.json，并同步验收/运维/契约边界。临时后端、浏览器和本轮 Vite 已结束，5198 端口已关闭；无部署或真实业务写入。真实知办安全卡片、防转发/重放/过期、公众号绑定和部署环境联调仍未完成。

## 第十阶段：工具 HTTP 入口与 HTTPS 约束（2026-09-14）

- 再次核对适用规则，MGS 仓库和父目录没有 AGENTS.md；知办 AGENTS.md 的部署协作规则不适用于本次只读检查。仅在 /opt/knowdo 的 apps/packages/services 查询约定路径、标识和开户相关实现，仍未找到本次契约的可联调接口；此结果不证明其他名称的通用 API 不存在。没有修改知办或发送跨任务消息。
- 发现 TrialServiceAuth 注释/契约要求 HTTPS，但通用验签入口未检查安全请求标志，只有私有凭据 Controller 检查。统一在所有 TrialCapability 入口验签前要求 request.isSecure；明文签名请求以及自行附加 X-Forwarded-Proto/Forwarded 均不能替代实际安全请求。没有启用全局转发头信任或改动运行代理。
- 新增 TrialToolHttpIntegrationTest 5 项回归，实际贯通 RequestBodyAdvice、生产 JSON 转换模块、Bean Validation、Controller、HMAC/nonce 数据库和本地 MGS 服务。验证四工具受理/只读/就绪区别，未确认不创建账号，状态查询不重放写入，指南使用精确持久化客户 ID；另覆盖身份归属、缺少确认、无签名、篡改、错误密钥能力、租户头、nonce 重放、越权/无效模型字段、超大请求，以及关闭新开户后仍能查询和维护到期记录。
- TrialServiceAuthTest 增加明文/伪造转发头拒绝测试。专项 11 项通过后，完整 run-tests.sh 通过 67 项试用测试 + 1 项 Springdoc；随后增加 OpenAPI HTTPS 扩展断言并单独重跑 Springdoc 通过。此前 30 项登录/OAuth 和 1 项真实隔离后端浏览器报告保留原执行时间，累计 99 项，不宣称全部在本轮重跑。
- 生成 OpenAPI 的服务安全说明及 x-mgs-service-contract 现在包含 transport=https、secureRequestRequired=true（布尔）；派生 tool-contract 同样已核对，仍为 12 个公开候选接口。更新对接契约和部署说明的可信代理条件。
- MockMvc secure 标志只证明应用入口约束生效，真实 TLS 握手、代理信任链、知办服务及公众号仍未联调。运行服务未部署、新开户未启用、没有真实业务写入。前端仍保持原有候选暂存状态，完整 checkType 门槛及其例外确认未解决。

## 第十一阶段：前端检查资源复核与运行文档观察（2026-09-14）

- 核对前端 package.json：实际 web-antd 的 typecheck 脚本硬编码 NODE_OPTIONS=--max-old-space-size=8192。为排除因堆上限过高导致回收不及时，在同一全应用 tsconfig 下直接运行相同 vue-tsc 参数，分别限制堆为 1792/2048 MiB，总内存仍 2560 MiB、交换 0、CPU 200%、180 秒上限。两次均明确因 JS 堆耗尽 SIGABRT 失败；进程已终止，不是仍在等待，也不是类型检查通过。没有改变源文件/检查范围/钩子或继续提高共享主机资源上限。
- frontend-test-evidence.json 记录两次命令、资源约束和失败日志。前端 10 个候选文件仍暂存，已有范围内回归证据保留；不把未得到答复的 checkType 例外当成授权。
- 只读 GET 运行实例的 /v3/api-docs 成功，文档标识 1.0.0-full-contract-v5，共 3324 路径，不包含 /crm/trial-。新增 runtime-observation.json 保存摘要、哈希和时间；不保存运行库内容，不推断运行 Git SHA，没有部署/真实业务写入。
- 核对 CRM 测试目录：本次 trial 目录之外只有原有测试资源，没有另一个可运行的旧 CRM 单测类集合；现有真实 CRM 服务回归由业务/联合/浏览器测试提供，不能报告一个不存在的旧 CRM 测试集通过。
- 本地候选测试证据仍为 99 项。完整目标的剩余阻断已连续存在：知办约定接口和真实安全交付/公众号联调条件缺失、企业/期限/配额/密钥配置未齐，前端完整类型检查需更多检查资源或明确候选提交例外。禁止修改其他项目/真实业务写入的边界下，不能自主完成剩余链路；也不能将本地候选完成改称完整体验完成。
